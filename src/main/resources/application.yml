spring:
  application:
    name: "cotizator"
  datasource:
    url: jdbc:h2:mem:cotizatordb
    driverClassName: org.h2.Driver
    username: user
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
b3:
  api:
    url: https://brapi.dev/api/
    token: cpSYJTvKcvWa4mosZ2uydd